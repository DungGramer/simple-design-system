import styles from './TextArea.module';
import PropTypes from 'prop-types';

const TextArea = ({}) => {
  return <div className={styles['text-area']}>TextArea</div>;
}

TextArea.propTypes = {};

export default TextArea;
