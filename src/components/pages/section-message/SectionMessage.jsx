import './SectionMessage.module';
import PropTypes from 'prop-types';

function SectionMessage(props) {
  return <div>SectionMessage</div>;
}

SectionMessage.propTypes = {};

export default SectionMessage;
